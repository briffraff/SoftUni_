using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;

namespace TrainingFile
{
    class Program
    {
        static void Main(string[] args)
        {
            //string[] dayofweek = { "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday" };

            //List<string> dayofweekList = new List<string>(dayofweek);

            //Console.WriteLine(string.Join(", ", dayofweek));
            //Console.WriteLine(string.Join(", ", dayofweekList));

            //dayofweekList.Add("Newday");
            //dayofweek = dayofweek.Concat(new string[] { "Newday" }).ToArray();

            //Console.WriteLine(string.Join(", ", dayofweek));
            //Console.WriteLine(string.Join(", ", dayofweekList));




            //string[] names = {"Gosho","Pesho","Tosho"};

            //string[] splitted = "Gosho, Pesho ,Tosho".Split(", ".ToCharArray(),StringSplitOptions.RemoveEmptyEntries);

            //splitted = splitted.Concat(new string[] {"Valio"}).ToArray();

            //for (int i = 0; i < splitted.Length; i++)
            //{
            //    Console.WriteLine(splitted[i]);
            //}


            //var assemblyChars = "".ToString();
            //string text = "GoshoPeshoTosho";
            //char[] textSplit = text.ToArray();

            //    for (int chars = 0; chars < 5; chars++)
            //    {
            //        assemblyChars = textSplit[chars].ToString();
            //        Console.Write(assemblyChars);
            //    }
            //Console.WriteLine();

            //string[] dayofweek = { "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday" ,"Yesterday" };

            //List<string> dayofweekList = new List<string>(dayofweek);

            //dayofweekList.Add("Newday");
            //string[] newdayofweek = dayofweekList.ToArray();
            
            //Console.WriteLine(string.Join(", ", dayofweek));
            //Console.WriteLine(string.Join(", ",newdayofweek));
        }
    }
}
